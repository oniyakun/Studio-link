{
  "name": "web-rtc-mic",
  "private": true,
  "scripts": {
    "start:signaling": "npm --workspace apps/signaling-server start",
    "start:sender-web": "npm --workspace apps/sender-web start"
  },
  "workspaces": [
    "apps/signaling-server",
    "apps/sender-web"
  ],
  "devDependencies": {
    "selfsigned": "^5.5.0"
  }
}
